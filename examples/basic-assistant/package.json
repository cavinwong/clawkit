{
  "name": "basic-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "A general-purpose personal AI assistant — ClawKit example",
  "type": "module",
  "scripts": {
    "dev": "clawkit dev",
    "build": "clawkit build",
    "pack": "clawkit pack",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clawkit/core": "workspace:*"
  },
  "devDependencies": {
    "@clawkit/cli": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}

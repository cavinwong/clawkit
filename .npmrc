# Registry configuration for @openclaw-kit scope
@openclaw-kit:registry=https://registry.npmjs.org/

# Allow publishing without 2FA prompt when using automation tokens
# (Granular Access Token with "bypass 2FA" enabled handles this automatically)
provenance=true

# Ensure workspace packages are resolved correctly during publish
# This converts workspace:* to actual version numbers

# Registry configuration for @openclaw-kit scope
@openclaw-kit:registry=https://registry.npmjs.org/

# Note: provenance is only supported in CI environments (GitHub Actions).
# It is intentionally NOT set here to allow local publishing.
# The release.yml workflow enables provenance via the id-token permission.
